-- SELECT COunt(*) 
-- from customers
-- ;
-- select COUNT(*) AS active_customers
-- from customers
-- where is_active = 1;

-- select count(distinct city_id) AS total_cities , count(customer_id) AS customers
-- from customers
-- order by customers; 

-- select sum(lifetime_value) AS total_lifetime_value
-- from customers
-- where is_active = 1;

-- select avg(lifetime_value) AS Avg_lifetime_value
-- from customers 
-- where membership_type = 'Gold';

-- select MAX(lifetime_value) AS max_lifetime_value
-- from customers
-- where is_active = 1;

-- select MIN(lifetime_value) AS min_lifetime_value
-- from customers
-- where membership_type = 'Gold';
